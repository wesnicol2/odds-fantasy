services:
  odds-fantasy:
    image: ghcr.io/wesnicol2/odds-fantasy:latest
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: ["python", "-m", "refactored.api", "--host", "0.0.0.0", "--port", "8000"]
    restart: unless-stopped
